<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit | <%= product?.name %>
        </title>
    </head>

    <body>
        <h1>Edit Product</h1>
        <form action="/products/<%= product._id %>?_method=PATCH" method="POST">
            <div>
                <label for="name">Product Name</label>
                <input type="text" name="name" id="name" placeholder="Name" value="<%= product.name %>">
            </div>
            <div>
                <label for="price">Product Price</label>
                <input type="numeric" min="0" name="price" id="price" placeholder="Price" value="<%= product.price %>">
            </div>
            <div>
                <label for="category">Product Category</label>
                <select id="category" name="category">
                    <option <%=product?.category==='fruit' ? 'selected' : '' %> >fruit</option>
                    <option <%=product?.category==='vegetable' ? 'selected' : '' %> >vegetable</option>
                    <option <%=product?.category==='dairy' ? 'selected' : '' %> >dairy</option>
                    <option <%=product?.category==='protein' ? 'selected' : '' %> >protein</option>
                </select>
            </div>
            <div>
                <input type="submit" value="SUBMIT">
            </div>
        </form>
    </body>

</html>